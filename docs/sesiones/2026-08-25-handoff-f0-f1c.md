# Handoff de sesión — 2026-08-25 (F0 → F1c)

> Documento de continuidad: permite retomar el trabajo en otra sesión con contexto mínimo.

## Estado del proyecto (qué está hecho y mergeado)

| Fase | Estado | PR |
|---|---|---|
| F0 Fundaciones (scaffold Payload 3.88 + Next 16, adapter Postgres, migraciones en build Vercel) | ✅ mergeado (#3) | — |
| F1 CRM core (users/clients/leads/activities/segments/documents/company-settings + RBAC + timeline join) | ✅ mergeado (#4) | — |
| F1b Multi-tenant (plugin oficial, tenant `martes` sembrado, company-settings por empresa) | ✅ mergeado (#5) | — |
| F1c Kanban pipeline (leads) vía `payload-kanban-board@3.0.24` | 🔵 PR abierto | #6 |
| F2 Dinero (payments/memberships + Resend básico + reminders + digest) | ⬜ siguiente | — |

## Infraestructura viva
- **Neon**: proyecto `martesapp` (`rapid-bonus-33572154`), PG18, rama default `br-wandering-block`, DB `neondb`. pgvector ACTIVADO. Esquema `neon_auth` residual sin uso (decidir si se elimina).
- **Vercel**: proyecto `martes-hub-nuevo` (cuenta angelpdi100-9571). Prod + preview desplegados, admin 200. Build = `pnpm migrate && pnpm build`.
- **Env vars** en los 3 entornos de Vercel: `PAYLOAD_SECRET`, `DATABASE_URL` (pooled), `DATABASE_URL_DIRECT` (nunca pooler para migraciones).
- Local `.env` en `~/martes-os/` (gitignored). Node requerido ≥24.15 → usar `nvm use 24.19.0`.

## Decisiones cerradas (detalle en README)
1. Adapter: `@payloadcms/db-postgres` (NO existe plugin Neon para Payload)
2. Neon standalone; todo acceso a BD vía Payload; operación BD solo por MCP (sin CLI)
3. Multi-tenant SaaS-ready operando mono-tenant; roles globales v1 (por-membresía diferido a fase SaaS)

## Pendientes inmediatos
1. **CSV import/export**: export nativo del list view de Payload 3.x ya cubre export; falta endpoint/plugin de IMPORT para clients/leads
2. **README "roadmap V3"**: el usuario dice haberlo actualizado pero NO está en el repo (ni main ni ramas) → localizar dónde lo editó
3. **PR #2 abierto es peligroso**: su rama es anterior a F1b y revertiría la decisión multi-tenant al mergearse → o se actualiza mezclando main dentro, o se cierra y se consolida en un PR nuevo de README v3
4. Revocar API key vieja de Neon (`napi_…`) en console.neon.tech → Account → Security (pendiente del usuario desde la config del MCP)
5. CI básico (typecheck+lint en GH Actions) — explícitamente pospuesto por el usuario

## Convenciones operativas (resumen)
- Commits convencionales; PRs obligatorios; identidad git local: `AngelDelN <57774536+aikapenelope@users.noreply.github.com>`
- Migraciones: `pnpm migrate:create <nombre>` usa wrapper `scripts/migrate.mjs` (fuerza URL directa); si drizzle pregunta interactivamente usar `expect` (ver `/tmp/opencode/f1b.exp` como plantilla)
- Tras cambiar colecciones: `pnpm generate:types` → `migrate:create` → `migrate` → typecheck/lint/smoke `/admin`
- MCP de opencode activos: Context7, Supabase, **Neon** (OAuth, tokens en `~/.local/share/opencode/mcp-auth.json`)
