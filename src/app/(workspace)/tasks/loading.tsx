export default function TasksLoading() { return <div className="workspace-page tasks-page" aria-busy="true"><div className="tasks-skeleton tasks-skeleton-head"/><div className="workspace-kpis tasks-metrics">{[1,2,3,4].map((item) => <div className="workspace-card tasks-skeleton tasks-skeleton-kpi" key={item}/>)}</div><div className="tasks-skeleton tasks-skeleton-toolbar"/><div className="tasks-board">{[1,2,3,4,5].map((item) => <div className="tasks-column tasks-skeleton tasks-skeleton-column" key={item}/>)}</div></div> }
