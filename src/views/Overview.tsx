/**
 * Named re-export de OverviewPage para el import map de Payload admin.
 * El view original vive en (workspace)/overview/page.tsx y sigue disponible
 * como ruta Next.js mientras el route group no sea eliminado.
 */
export { default as OverviewView } from '../app/(workspace)/overview/page'
